node_modules
dist
.env
.env.local
.env.*.local
TODO.md

.playwright-mcp/
worker/.wrangler/

# Tooling workspaces — can contain credentials, site links, or build artifacts
.netlify/
.vercel/
.cloudflare/
*.local

# Credential files that might get dropped in the root accidentally
*credentials*.json
*service-account*.json
*.pem
*.p12
*secret*.json

# Internal audit reports — keep local only, do NOT commit to public repo
security_best_practices_report.md
